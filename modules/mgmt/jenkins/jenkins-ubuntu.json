{
  "variables": {
    "service_catalog_ref": null,
    "version_tag": null,
    "aws_region": "us-east-1",
    "ami_name": "jenkins-server",
    "copy_to_regions": "",
    "github_auth_token": "{{env `GITHUB_OAUTH_TOKEN`}}",
    "encrypt_boot": "false",
    "vpc_filter_key": "isDefault",
    "vpc_filter_value": "true",
    "vpc_subnet_filter_key": "default-for-az",
    "vpc_subnet_filter_value": "true",
    "ami_users": "",
    "jenkins_version": "",
    "module_security_version": "",
    "module_aws_monitoring_version": "",
    "module_stateful_server_version": "",
    "module_ci_version": "",
    "kubergrunt_version": "",
    "bash_commons_version": "",
    "terraform_version": "",
    "terragrunt_version": "",
    "kubectl_version": "",
    "helm_version": "",
    "packer_version": "",
    "docker_version": "",
    "enable_ssh_grunt": "true",
    "enable_cloudwatch_metrics": "true",
    "enable_cloudwatch_log_aggregation": "true"
  },
  "sensitive-variables": ["github_auth_token"],
  "builders": [{
    "ami_name": "{{user `ami_name`}}-{{isotime | clean_resource_name}}",
    "ami_description": "An Ubuntu AMI that runs Jenkins.",
    "instance_type": "t3.micro",
    "region": "{{user `aws_region`}}",
    "type": "amazon-ebs",
    "source_ami_filter": {
      "filters": {
        "virtualization-type": "hvm",
        "architecture": "x86_64",
        "name": "ubuntu/images/hvm-ssd/ubuntu-bionic-18.04-amd64-server-*",
        "block-device-mapping.volume-type": "gp2",
        "root-device-type": "ebs"
      },
      "owners": ["099720109477"],
      "most_recent": true
    },
    "vpc_filter": {
      "filters": {
        "{{user `vpc_filter_key`}}": "{{user `vpc_filter_value`}}"
      }
    },
    "subnet_filter": {
      "filters": {
        "{{user `vpc_subnet_filter_key`}}": "{{user `vpc_subnet_filter_value`}}"
      },
      "most_free": "true"
    },
    "ssh_username": "ubuntu",
    "encrypt_boot": "{{user `encrypt_boot`}}",
    "ami_users": "{{user `ami_users`}}",
    "ami_regions": "{{user `copy_to_regions`}}",
    "tags": {
      "service": "{{ user `ami_name` }}",
      "version": "{{user `version_tag`}}"
    }
  }],
  "provisioners": [{
    "type": "shell",
    "inline": [
      "sudo DEBIAN_FRONTEND=noninteractive apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get -y upgrade && sudo DEBIAN_FRONTEND=noninteractive apt-get -y install jq curl",
      "curl -Ls https://raw.githubusercontent.com/gruntwork-io/gruntwork-installer/master/bootstrap-gruntwork-installer.sh | bash /dev/stdin --version v0.0.22",
      "gruntwork-install --module-name mgmt/jenkins --repo https://github.com/gruntwork-io/aws-service-catalog --tag {{user `service_catalog_ref`}}"
    ],
    "environment_vars": [
      "GITHUB_OAUTH_TOKEN={{user `github_auth_token`}}",
      "jenkins_version={{user `jenkins_version`}}",
      "module_security_version={{user `module_security_version`}}",
      "module_aws_monitoring_version={{user `module_aws_monitoring_version`}}",
      "module_stateful_server_version={{user `module_stateful_server_version`}}",
      "module_ci_version={{user `module_ci_version`}}",
      "kubergrunt_version={{user `kubergrunt_version`}}",
      "bash_commons_version={{user `bash_commons_version`}}",
      "terraform_version={{user `terraform_version`}}",
      "terragrunt_version={{user `terragrunt_version`}}",
      "kubectl_version={{user `kubectl_version`}}",
      "helm_version={{user `helm_version`}}",
      "packer_version={{user `packer_version`}}",
      "docker_version={{user `docker_version`}}",
      "enable_ssh_grunt={{user `enable_ssh_grunt`}}",
      "enable_cloudwatch_metrics={{user `enable_cloudwatch_metrics`}}",
      "enable_cloudwatch_log_aggregation={{user `enable_cloudwatch_log_aggregation`}}"
    ],
    "pause_before": "30s"
  }]
}
